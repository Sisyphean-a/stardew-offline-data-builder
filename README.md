# stardew-offline-data-builder

`stardew-offline-data-builder` is a Python 3.12 CLI project scaffold for the
Stardew Valley offline data builder.

Current status: only Phase 0 project initialization is implemented.

Run:

```powershell
python -m builder --help
```
