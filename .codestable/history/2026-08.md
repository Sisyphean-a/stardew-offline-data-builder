- 2026-08-09 · [重构] 将旧 goals/issues/audits/reference 等过程资料迁移为按范围加载的 architecture、requirements、ADR、history 结构；已吸收事实保留当前代码锚点与 Git 可追溯证据，旧过程目录不再作为默认入口。唯一未闭合的 Steam 验收原件仍保留在 `.codestable/features/2026-07-17-steam-game-discovery/`，不作为当前设计依据，待用户确认后再清理。范围：workspace
  原因：新会话应直接找到当前设计、领域规则和变化原因，避免旧的完成声明、门禁模板和运行时资料与当前代码形成双轨；未获用户确认的验收不能擅自改成已接受。
  当前依据：`.codestable/attention.md`、`.codestable/architecture/INDEX.md`、`.codestable/requirements/CONTEXT.md`
  证据：迁移前 `.codestable/` 树可由 Git 恢复；当前代码与 `PROJECT.md`、`README.md` 保持一致；Steam 原件 `steam-game-discovery-acceptance.md` 仍为 `status: pending`。

- 2026-08-09 · [功能] 将官方 SpriteSheet 图片物化调整为更接近 Lookup Anything 的静态绘制效果：村民 Portraits 只裁剪首个 64×64 单元，角色 fallback 使用实际 Characters 画布；SpriteSheet 保留声明画布并使用最近邻采样。范围：workspace
  原因：Lookup Anything 运行时从游戏 Content 的 Portraits、springobjects、Craftables、家具等纹理按源矩形绘制；原构建产物曾把 Abigail 整张头像表打包为缩略图，且透明边缘裁剪改变了物品和大型物品的原始画布。
  当前依据：当前图片物化代码与官方 Content 解包资源；应用继续只消费数据包 `image_path`，不读取游戏资源或改变 SQLite schema。
  证据：`src/builder/parsers/legacy_visuals.py`、`src/builder/pipeline/images.py`、`src/builder/utils/images.py` 及对应测试；`python -m pytest` 105 项通过，`python -m ruff check .` 通过；真实独立产物 `build/.tmp-real-followup/` 质量通过且 `dataErrors=0`，Abigail 为 64×64、Old-Mariner fallback 为 16×32、普通物品为 16×16、大型可制作物为 16×32、成就为 64×64、家具样本为 16×32。超大家具仍受现有最长边 96 的缩略图上限约束。

- 2026-08-09 · [缺陷] 修复图鉴发布数据中的技术标题、错误图片裁剪与跨实体图片来源追踪。范围：workspace
  原因：真实包中的商店为空标题、NPC 日程显示 Abigail:16、掉落记录无法区分；工具使用 SpriteIndex 而非菜单图标，作物静态裁剪得到透明格，Junimo 壁炉默认源矩形错误；跨实体继承图片还必须保留官方 provenance。
  当前依据：`PROJECT.md` 质量要求、schema 4 数据包契约及当前构建/图片 pipeline；app 仍只消费发布包，不解析游戏资源。
  证据：`src/builder/pipeline/normalize.py` 及 `normalize_*.py`、`src/builder/pipeline/provenance.py`、`src/builder/parsers/official_visuals.py`、`src/builder/parsers/legacy_visuals.py`、`src/builder/pipeline/images.py`；真实独立产物 `build/.tmp-ui-followup-real/` 共 3688 实体，目标标题均唯一且非空，必需图片 984/984，质量 passed、errors 为空、SQLite integrity 为 ok、ZIP 2605 项 CRC 通过；`python -m pytest` 118 项和 `ruff check .` 通过。
