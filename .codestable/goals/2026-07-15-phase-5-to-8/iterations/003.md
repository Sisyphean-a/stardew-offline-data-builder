---
doc_type: goal-iteration
goal: "phase-5-to-8"
iteration: 3
status_after: active
next_action: "继续阶段 7：把新增实体类型接入真实 build fixture 与数据库验证，再决定阶段 7 的提交边界。"
blocker_signature: null
updated_at: "2026-07-15"
---

# Iteration 003

## Current Understanding

阶段 6 已完成，阶段 7 的完整目标很大，当前需要先让代码库具备“更多实体类型可被发现和解析”的基础能力，再逐步把它们接进真实构建流程。

## Implementation Approach

- 先不假设真实游戏的复杂字段结构。
- 先把文件发现和通用本地化 JSON 解析能力扩展到阶段 7 列出的更多实体类别。
- 用虚构 fixture 验证这些类别至少能进入 `RawEntity` 流程，不破坏现有构建链路。

## Changes This Iteration

- 扩展 `infer_entity_type` 的关键词映射，覆盖：
  - `villager_gift`
  - `npc_schedule`
  - `cooking_recipe`
  - `crafting_recipe`
  - `shop`
  - `quest`
  - `bundle`
  - `monster`
  - `drop`
  - `mineral`
  - `weapon`
  - `ring`
  - `achievement`
  - `special_order`
  - `ginger_island`
- `game_source` 现在对未显式专用解析器的类型自动走通用 localized parser。
- 新增 `tests/test_phase7_generic_types.py`，验证阶段 7 类别可进入 `RawEntity` 流程。
- 扩展 `tests/test_match.py`，验证阶段 7 类别的文件名识别。

## Verification Evidence

- `pytest`：`19 passed`
- `ruff check .`：`All checks passed!`
- `tests/test_phase7_generic_types.py` 已验证 `monster`、`weapon`、`achievement` 的通用解析。
- `tests/test_match.py` 已验证多个阶段 7 类别的文件名识别。

## Problems Encountered

- 阶段 7 的真实游戏文件结构在 `PROJECT.md` 里没有逐类细化，当前实现只推进到“可识别、可进入通用解析流程”，没有伪装成已经掌握全部真实结构。

## Next Attempt

继续阶段 7，把新增类型接入真实 fixture 和数据库构建验证，逐步逼近“完整数据扩展”。

## State Update

- `state.yaml.status = active`
- `current_iteration = 3`
- 阶段 7 已开始，但尚未达到“整阶段完成”的提交边界
