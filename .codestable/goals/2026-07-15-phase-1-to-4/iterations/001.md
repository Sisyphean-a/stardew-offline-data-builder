---
doc_type: goal-iteration
goal: "phase-1-to-4"
iteration: 1
status_after: complete
next_action: "已完成阶段 1 到阶段 4，实现与验收均已结束。"
blocker_signature: null
updated_at: "2026-07-15"
---

# Iteration 001

## Current Understanding

目标是把项目从阶段 0 推到阶段 4 完成，并且严格停在阶段 4，不进入阶段 5 的社区数据合并与后续打包、图片、Android 范围。

## Implementation Approach

- 阶段 1：先实现 fixture 驱动的数据闭环，打通 `build-fixture`、标准化、拼音搜索字段、SQLite 写入与 `inspect`。
- 阶段 2：补 `doctor`，统一路径校验与 SQLite FTS4 检查。
- 阶段 3：补 `unpack`，统一外部进程调用、跳过已有结果和 `--force`。
- 阶段 4：补四类官方中文 JSON 解析与发现逻辑，并以虚构夹具覆盖。

## Changes This Iteration

- 增加项目依赖：`orjson`、`pydantic`、`pypinyin`、`rich`。
- 实现数据模型 `RawEntity`、`NormalizedEntity`、`SearchDocument`、`BuildSummary`。
- 实现 `build-fixture`、`inspect`、`doctor`、`unpack` 四个 CLI 命令。
- 实现四类官方 JSON 文件发现与解析：普通物品、作物、鱼类、村民。
- 实现标准化、拼音搜索字段生成、SQLite schema 与写库/检视逻辑。
- 增加虚构 fixture 数据与 12 个测试用例。
- 修复 Windows GBK 控制台输出问题，CLI 启动时统一配置 UTF-8 输出。
- 修复默认 `StardewXnbHack` 探测逻辑，优先 `.exe`，回退 `.py`。
- 按阶段补了 4 次提交，并追加 1 次兼容性修复提交：
  `3cf2ed6`、`b509a8d`、`7c0863b`、`e9b32d4`、`f1d030a`。

## Verification Evidence

- `python -m builder build-fixture --output .\dist`
- `python -m builder inspect .\dist\stardew.db`
- `pytest`
- `ruff check .`
- 手动构造中文/空格路径游戏目录，执行 `python -m builder doctor --game-dir ... --community-data ...`
- 手动构造临时解包工具，执行 `python -m builder unpack --game-dir ... --force`
- 独立只读 Task agent 功能验收，结果为 `pass`

## Problems Encountered

- 并行运行 `pip install`、`pytest`、`ruff` 时出现过一次依赖可见性竞争，改为串行验证后消失。
- Windows GBK 控制台最初无法输出 `✓` / `✗`，后改为 CLI 启动时重配 `stdout/stderr` 为 UTF-8。
- `PROJECT.md` 示例偏向 `.exe`，初版默认探测偏向 `.py`，已修正为 `.exe` 优先、`.py` 回退。

## Next Attempt

无。目标已完成。

## State Update

- `state.yaml.status = complete`
- `current_iteration = 1`
- 功能验收记录已写入 `functional-acceptance.md`
